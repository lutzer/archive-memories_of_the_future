define([
        'underscore',
        'backbone',
        'values/constants'
], function(_, Backbone, Constants){

	var RecordModel = Backbone.Model.extend({
		
	});

	// Return the model for the module
	return RecordModel;

});